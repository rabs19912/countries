const detailsFilters =
  "fields=name,capital,flags,region,population,subregion,borders,languages,nativeName,tld,currencies";

const BASE_URL = "https://restcountries.com/v3.1";

const filters = "fields=name,capital,flags,region,population,cca2";
const regions = ["Africa", "America", "Asia", "Europe", "Oceania"];

export {
    BASE_URL,
    filters,
    detailsFilters,
    regions
}
